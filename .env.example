# PostgreSQL
POSTGRES_USER=grafikowy
POSTGRES_PASSWORD=changeme
POSTGRES_DB=grafikowy
DATABASE_URL=postgresql+asyncpg://grafikowy:changeme@grafikowy-db:5432/grafikowy

# JWT
JWT_SECRET=generate-a-random-secret-here
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# CORS
CORS_ORIGINS=http://localhost:5173,https://grafikowy.patrz.byst.re
